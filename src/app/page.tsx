export default function HomePage() {
  return (
    <div>
      <h1>Loyalty Punch</h1>
      <p>This is a placeholder home page. Real entry points are:</p>
      <ul>
        <li>
          <code>/t/[fobId]</code> — customer fob tap status page
        </li>
        <li>
          <code>/dashboard</code> — owner dashboard
        </li>
      </ul>
    </div>
  );
}
